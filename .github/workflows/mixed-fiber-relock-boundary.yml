name: mixed-fiber-relock-boundary

on:
  push:
    branches: [agent/geometric-wave-state-v01]
    paths:
      - 'map/mixed_fiber_compiler_probe.py'
      - 'map/mixed_fiber_noise_boundary.py'
      - 'map/mixed_fiber_relock_boundary.py'
      - 'tests/test_mixed_fiber_relock_boundary.py'
      - '.github/workflows/mixed-fiber-relock-boundary.yml'
  workflow_dispatch:

jobs:
  boundary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --upgrade pip
      - run: pip install -r requirements.txt
      - name: Focused tests
        run: pytest -q tests/test_mixed_fiber_relock_boundary.py
      - run: mkdir -p results
      - name: Sweep digital relock interval
        run: python map/mixed_fiber_relock_boundary.py --json > results/mixed_fiber_relock_boundary.json
      - uses: actions/upload-artifact@v4
        with:
          name: mixed-fiber-relock-boundary
          path: results/mixed_fiber_relock_boundary.json
